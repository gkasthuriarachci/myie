import React from "react";

const Header = () => {
  return (
    <div>
      <h1>Credit Card application</h1>
      <p>Please complete the following, it should only take a few minutes.</p>
      <p> All information is required unless otherwise specified.</p>
      <p>
        During the application you can click <strong>Save for later</strong> to
        store your details and then return and continue later.
      </p>
    </div>
  );
};

export default Header;
